# V6.7 Notes

## Entregas
- CLI avançado com mission / health / memory / rollback
- API real com:
  - GET /api/health
  - POST /api/mission
  - GET /api/memory
  - GET /api/memory/<mission_id>
  - POST /api/rollback
  - POST /api/rollback-file
- SQLite memory com queries
- rollback por snapshot e seletivo por arquivo
